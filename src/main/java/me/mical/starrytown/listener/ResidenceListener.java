package me.mical.starrytown.listener;

// import com.bekvon.bukkit.residence.event.ResidenceDeleteEvent;
import me.mical.starrytown.ConfigReader;
import me.mical.starrytown.data.Town;
import org.bukkit.event.EventHandler;
import org.bukkit.event.Listener;

import java.util.*;

/**
 * @author xiaomu
 * @since 2022/9/10 17:10
 */
public class ResidenceListener implements Listener {
/*
    @EventHandler
    public void e(final ResidenceDeleteEvent e) {
        final Map<UUID, Town> edit = new HashMap<>();
        ConfigReader.TOWNS.values().stream().filter(town -> Objects.equals(town.getResidence(), e.getResidence().getName())).forEach(town -> edit.put(town.getUuid(), town));
        ConfigReader.TOWNS.putAll(edit);
    }

 */
}
